# 开发者日志 —— "简简单单的新闻"

> 一个多源新闻聚合桌面工具，从想法到交付的全过程记录。

---

## 缘起

朋友想要一个能自动收集国内各大新闻媒体每日发布的工具，要求：
- 朋友下载后双击就能用，不需要任何配置
- 覆盖中国主流媒体（新华社、人民日报、央视等）
- 能标记已读、收藏、搜索
- AI 自动生成摘要
- 看到好文章能一键保存全文

---

## 第一阶段：需求厘清

### 问题：用户不知道自己想要什么

一开始的需求是"做一个新闻收集工具"，但什么形态、给谁用、怎么用都不清楚。

**方法：** 用 `/grill-me` 技能逐项拷问，从目标用户、新闻源、采集频率、输出形态、数据模型到界面功能，一个问题一个问题地收窄。

**关键决策：**
- 本地 exe，不是网站（朋友不需要服务器）
- 每个用户各自独立，无需登录
- SQLite 本地存储，不是云数据库
- 7 家中国媒体，RSS + 首页爬虫双模式
- DeepSeek API 做 AI 摘要

---

## 第二阶段：中文新闻源的现实

### 问题：RSS 大面积失效

一开始天真地以为中文媒体都有 RSS 可用，结果逐一测试后发现：

| 媒体 | RSS | 实际情况 |
|------|-----|----------|
| 新华社 | 有 | 返回 403 禁止访问 |
| 央视新闻 | 有 | 404 不存在 |
| 澎湃新闻 | 有 | 302 重定向到非 RSS 页面 |
| 虎嗅 | 有 | 阿里云 WAF 防护，无法爬取 |
| 财新 | 有 | 302 重定向 |
| 36氪 | **能用** | 唯一确认可用的 RSS |
| 人民日报 | **能用** | OPML 格式，可用 |
| 新浪新闻 | 有 | 返回 HTML 非 RSS |

**解决方法：** 抛弃纯 RSS 方案，改为 **RSS + 首页爬虫双模式**。RSS 可用则用 RSS，否则抓取首页 HTML，通过正则匹配新闻链接。

虎嗅有 WAF 防护，直接移除。最终 7 个源。

### 问题：首页爬虫的噪音

首页 HTML 里有导航链接、广告、推荐阅读等大量非新闻链接。

**解决方法：** 为每个源定义 `url_pattern`，用正则过滤出真正的新闻链接。例如新华社的链接包含 `/politics/`、`/local/` 等路径特征，新浪新闻的链接包含 `/doc-` 前缀。

---

## 第三阶段：UI 设计

### 问题：应该做什么风格

一开始做了深色主题，朋友看了另一个项目 dailynewsflow 后更喜欢 Apple 风格的亮色设计。

**解决方法：** 全面重写 CSS/HTML，从 dailynewsflow 借鉴：
- Apple 风格系统字体栈
- 亮色为主，暗色作为可切换选项
- 卡片式布局，圆角、阴影
- 无衬线 + 衬线字体搭配

### 问题：命名

一开始叫"新闻早报"，朋友觉得太普通，改为"简简单单的新闻"。

---

## 第四阶段：深度爬取功能

### 问题：SPA 页面抓不到内容

36氪是 Nuxt.js 渲染的 SPA，内容嵌入在 `window.__NUXT__` 中，传统的 BeautifulSoup 解析拿不到正文。

**解决方法：** 改进 `fetch_article` 函数，增加多层兜底策略：
1. 找 `<article>` 语义标签
2. 找常见 class/id 容器
3. 取所有长 `<p>` 标签
4. **提取 `window.__NUXT__` 中嵌入的数据**
5. 兜底取所有可见文本

### 问题：DeepSeek JSON 解析

DeepSeek 返回的内容有时带 markdown 代码块标记（```json），有时直接返回纯文本。

**解决方法：** 在解析前清理 markdown 标记，加上 `removeprefix` / `removesuffix` 处理。

---

## 第五阶段：API Key 的安全问题

### 问题：Key 不能公开

朋友的 Key 如果硬编码在源代码里，推到 GitHub 上就公开了。但如果每个朋友都要自己申请 Key，体验太差。

**解决方法（三層防護）：**
1. **环境变量注入**：GitHub Actions 构建时从 Secrets 读取 Key
2. **`_secret.py` 已 gitignore**：本地开发用独立文件，不进版本控制
3. **运行时优先级**：环境变量 > `_secret.py` > `config.json`

这样源码里永远看不到 Key，但编译出的 exe 里内置了 Key，用户开箱即用。

### 问题：CI 构建失败

GitHub Actions 构建一直失败，但看不到错误日志。

**根因：** 创建 `_secret.py` 的那行 Python 命令在 shell 中转义出了问题：
```bash
# 错误的写法（shell 把 \" 当成了字面反斜杠+引号）
python -c "... __import__(\"os\").environ[\"KEY\"] ..."

# 正确的写法（用 heredoc 避免转义问题）
python << 'PYEOF'
import os
with open('src/_secret.py', 'w') as f:
    f.write('DEEPSEEK_API_KEY = ' + repr(os.environ['KEY']))
PYEOF
```

教训：CI 环境下的 shell 转义不能靠猜，用 heredoc 最安全。

---

## 第六阶段：PyInstaller 打包

### 问题：模板文件找不到

PyInstaller 打包后运行时，Flask 报 `TemplateNotFound`。原因是 `--add-data` 的目标路径不对。

**解决方法：** 目标路径必须匹配 Flask 在 PyInstaller 环境下的模块路径。`app.py` 在 `src/` 下，模板应映射到 `src/templates`。

```bash
# 错误：模板放在 _internal/templates/
--add-data "src/templates;templates"

# 正确：模板放在 _internal/src/templates/
--add-data "src/templates;src/templates"
```

### 问题：onedir vs onefile

`--onedir` 模式输出文件夹（34MB），`--onefile` 模式输出单个 exe（19MB）。

**选择：** `--onefile`。虽然启动时多一次自解压，但朋友只需要下载一个文件，体验更好。

---

## 最终架构

```
简简单单的新闻/
├── src/                          # Python 源码
│   ├── __main__.py               # 入口：启动服务器 + 打开浏览器
│   ├── app.py                    # Flask Web 应用 + API 路由
│   ├── collector.py              # RSS + 首页爬虫采集器
│   ├── crawler.py                # 深度爬取 + AI 分析
│   ├── models.py                 # SQLite 数据模型
│   ├── config.py                 # 配置管理（Key 读取）
│   ├── sources.py                # 7 个新闻源定义
│   ├── summarizer.py             # DeepSeek 摘要生成
│   ├── _secret.py                # 内置 API Key（已 gitignore）
│   ├── static/                   # CSS + JS
│   └── templates/                # HTML 模板
├── .github/workflows/build.yml   # CI 自动构建
├── build.py                      # PyInstaller 打包脚本
├── README.md
└── 开发者日志.md
```

### 数据流

```
新闻源 → RSS/爬虫 → SQLite 存储 → Flask API → 浏览器展示
                         ↓
                   DeepSeek API → AI 摘要/分析
                         ↓
                   爬取全文 → 保存为 .txt 文件
```

---

## 发布

最终发布链接：
```
https://github.com/Rainier-Z/very-simple-news/releases/download/v1.0/news-collector-win.exe
```

19MB 单文件，双击即用，无需任何配置。
